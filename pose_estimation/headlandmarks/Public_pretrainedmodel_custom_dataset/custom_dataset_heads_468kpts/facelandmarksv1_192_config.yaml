general:   
   num_threads_tflite: 8

model:
   model_type: head_spe
   model_path: ../../stm32ai-modelzoo/pose_estimation/headlandmarks/Public_pretrainedmodel_custom_dataset/custom_dataset_heads_468kpts/facelandmarksv1_192_int8.tflite

operation_mode: prediction

dataset:
   class_names: [head]
   keypoints: 468

preprocessing:
   rescaling:
      scale: 1/255.
      offset: 0
   resizing:
      interpolation: bilinear
      aspect_ratio: fit
   color_mode: rgb

postprocessing:
   kpts_conf_thresh: 0.15

prediction:
   test_files_path: ./datasets/head_test_single_pose

mlflow:
   uri: ./src/experiments_outputs/mlruns

hydra:
   run:
      dir: ./src/experiments_outputs/${now:%Y_%m_%d_%H_%M_%S}
